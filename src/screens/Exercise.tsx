import { Box, Center, Text } from "native-base";

export function Exercise() {
    return (
      <Box flex={1}>
        <Center flex={1}>
          <Text color={'white'}>Exercise</Text>
        </Center>
      </Box>
    );
}